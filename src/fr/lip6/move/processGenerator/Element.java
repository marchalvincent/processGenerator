package fr.lip6.move.processGenerator;

/**
 * Cette interface ne permet que de simplifier la manipulation des différentes 
 * enum d'éléments (bpmn, uml, etc.).
 * @author Vincent
 *
 */
public interface Element {}
